
1、重定向和管道的区别？
	简单地说，重定向操作符>将命令与文件连接起来，用文件来接收命令的输出；
	而管道符|将命令与命令连接起来，用第二个命令来接收第一个命令的输出
	
2、查看指定程序httpd的进程运行状态，并将输出重定向到文件中。
	ps aux | grep httpd > /tmp/ps.output
	
3、统计系统中当前登录的用户数。
	who | wc -l

4、使用 mount 命令显示当前挂载的文件系统的信息，并使用 column 命令格式化列的输出，最后将输出结果保存到一个文件中。
	mount | column -t >mounted.txt